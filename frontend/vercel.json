{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "buildCommand": "npm run build && cp -r src/assets dist/",
    "outputDirectory": "./dist",
    "installCommand": "npm install"
}